---
tags: [workflow, nomenclature]
---

# Workflow

The full journey of one feature, from idea to merged code. A workflow runs in three stages — Spec, Plan, Implement — so "workflow" names the whole pipeline, never a single stage. The CLI's own help text still says "spec workflow" for one stage; that is legacy wording, not this definition.
