schema: 3
written_by: 0.1.0
name: current
agent: claude
skills_version: 0.1.0
spec:
    config:
        directory: specs
plan:
    config:
        directory: plans
changelog:
    config:
        directory: changelog
repos:
    - name: current
      location: .
